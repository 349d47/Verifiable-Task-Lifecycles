# Environment

The archived environment records Python 3.13.15 and the package versions pinned in `requirements.txt`. The paper reports Foundry `forge` v1.8.1.

A shell capture in this archive did not have `forge` on its PATH and is not used as environment evidence.
