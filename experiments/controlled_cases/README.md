# Controlled Cases

`cases.json` contains the six cases reported in Table II.

- T1-T5 test lifecycle admission for RQ1.
- T6 tests post-recording Evidence modification.

The public test suite adds six binding and control checks.

```bash
python -m unittest discover -s tests -v
```
